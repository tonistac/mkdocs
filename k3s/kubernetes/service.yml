apiVersion: v1
kind: Service
metadata:
  name: healthpulse-service
  namespace: healthpulse-prod    # Change per environment
  labels:
    app: healthpulse
spec:
  type: LoadBalancer
  selector:
    app: healthpulse
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP